import { LoadingScreen } from "@/components/loading-screen";

// K2·F175 — 페이지 이동 안내 통일 (앱 내 알고가기 진입)
export default function Loading() {
  return <LoadingScreen message="알고가기를 시작합니다" showSprout />;
}
